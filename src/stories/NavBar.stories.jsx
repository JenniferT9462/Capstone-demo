import NavBar from '../components/NavBar';

const meta = {
  component: NavBar,
};

export default meta;

export const Default = {
  args: {
    item1: 'Home',
    item2: 'Calendar',

  }
};

export const Primary = {
  args: {
    variant: 'primary',
  },
};